{# mk an orphan tweet page pulling data from json file #}

---
permalink: "tweets/index.html"
---

<h1>All Tweets</h1>

{% for tweet_id in twids %}
<article class="tweet">
  <p><strong>@{{ users[tweets[tweet_id].user_id].screen_name }}</strong></p>
  <p>{{ tweets[tweet_id].text }}</p>
  <p><small>{{ tweets[tweet_id].created_at }}</small></p>
</article>
{% endfor %}


{#
{% for tweet in tweets %}
<article class="tweet">
  <p><strong>@anonymous</strong></p>
  <p>{{ tweet.text }}</p>
  <p><small>{{ tweet.created_at }}</small></p>
  
  {% if tweet.retweeted_status %}
    <blockquote>
      <p>{{ tweet.retweeted_status.text }}</p>
      <p><small>{{ tweet.retweeted_status.created_at }}</small></p>
    </blockquote>
  {% endif %}
</article>
{% endfor %}
#}